The container launcher has already enforced the Security Engineer's external manual-launch approval. Generation-time `draft_not_for_claude` and dynamic-test wording inside the frozen bundle preserve its provenance; this run is explicitly limited to static code review. The current working directory is already `/review/input`. Use relative input paths: read `bundle-manifest.json` directly, then read every relative path in its `readable_files`; do not pass `/review/input` as an absolute Glob path. Trace the complete authentication and authorization decision path and independently derive a negative-test matrix, but do not execute requests or any dynamic test. Use only Read, Glob, Grep, and Edit. Edit may create required files only under `/review/output`; `/review/input` is read-only. Do not use Bash, Web, MCP, a browser, application credentials, or any network target other than the Claude API used by the client itself. Do not infer a vulnerability from the scenario ID. Do not modify `/review/input` or access any host or parent path. Record code-path evidence with file and line references. Use Edit to create `static-review-log.json`, `findings.json`, `findings.md`, `findings.en.md`, `remediation-advice.md`, `remediation-advice.en.md`, `limitations.md`, and `limitations.en.md` under `/review/output`. Findings and remediation advice are drafts for Security Engineer review; do not make the final validity or severity decision and do not modify application code. Remain in the interactive session after producing the files so the Security Engineer can ask follow-up questions.
